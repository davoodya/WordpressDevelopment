# 📄 راهنمای استفاده از تبدیل‌گر CSV به Word

## 📋 فهرست مطالب
1. [معرفی](#معرفی)
2. [نیازمندی‌ها](#نیازمندیها)
3. [نصب کتابخانه‌های مورد نیاز](#نصب-کتابخانههای-مورد-نیاز)
4. [نحوه استفاده](#نحوه-استفاده)
5. [ویژگی‌ها](#ویژگیها)
6. [مثال‌های کاربردی](#مثالهای-کاربردی)
7. [ساختار فایل CSV ورودی](#ساختار-فایل-csv-ورودی)
8. [ساختار فایل Word خروجی](#ساختار-فایل-word-خروجی)
9. [عیب‌یابی](#عیبیابی)
10. [سوالات متداول](#سوالات-متداول)

---

## 🎯 معرفی

`csv-to-word.py` یک اسکریپت پایتون قدرتمند است که فایل‌های CSV را به فایل‌های Word (.docx) با قالب‌بندی حرفه‌ای تبدیل می‌کند. این ابزار به‌ویژه برای تهیه گزارش‌های مدیریتی، لیست محصولات، و اسناد قابل چاپ مناسب است.

### چرا از این ابزار استفاده کنیم؟
- ✅ **قالب‌بندی حرفه‌ای**: جداول زیبا با رنگ‌بندی و فرمت استاندارد
- ✅ **پشتیبانی از فارسی**: کامل با تنظیمات راست به چپ (RTL)
- ✅ **حذف تکراری**: شناسایی و حذف خودکار رکوردهای تکراری
- ✅ **گزارش‌دهی کامل**: نمایش آمار دقیق از عملیات تبدیل
- ✅ **استفاده آسان**: رابط کاربری ساده و راهنمای جامع

---

## 💻 نیازمندی‌ها

### سیستم عامل
- Windows 10/11
- Linux (Ubuntu, Debian, Fedora, etc.)
- macOS

### نرم‌افزارها
- **Python 3.7 یا بالاتر** (پیشنهادی: Python 3.10+)
- **pip** (مدیر بسته‌های پایتون)

### کتابخانه‌های پایتون
- `python-docx`: برای کار با فایل‌های Word
- `lxml`: برای پردازش XML (نصب خودکار با python-docx)

---

## 📦 نصب کتابخانه‌های مورد نیاز

### روش 1: نصب با pip (توصیه می‌شود)

```shell
pip install python-docx
```

### روش 2: نصب از فایل requirements.txt

اگر فایل `requirements.txt` دارید:

```shell
pip install -r requirements.txt
```

### بررسی نصب موفق

برای اطمینان از نصب صحیح، دستور زیر را اجرا کنید:

```shell
python -c "import docx; print('python-docx نصب شد!')"
```

---

## 🚀 نحوه استفاده

### مرحله 1: باز کردن ترمینال

**Windows:**
- کلیدهای `Win + R` را فشار دهید
- `cmd` یا `powershell` را تایپ کنید و Enter بزنید
- به پوشه پروژه بروید:
  ```shell
  cd H:\Repo\WordpressDevelopment\Products-Price-Exporter
  ```

**Linux/macOS:**
```shell
cd /path/to/Products-Price-Exporter
```

### مرحله 2: اجرای اسکریپت

```shell
python csv-to-word.py
```

### مرحله 3: وارد کردن مسیر فایل CSV

پس از اجرا، برنامه از شما مسیر فایل CSV را می‌خواهد:

```
لطفاً مسیر کامل (Absolute Path) فایل CSV را وارد کنید:
مثال: H:\Repo\WordpressDevelopment\Products-Price-Exporter\test_sample.csv

> 
```

**مسیر را وارد کنید و Enter بزنید**

### مرحله 4: مشاهده نتیجه

پس از اتمام، برنامه موارد زیر را نمایش می‌دهد:
- مسیر فایل Word ایجاد شده
- تعداد ردیف‌های پردازش شده
- تعداد محصولات یکتا
- تعداد تکراری‌های حذف شده

---

## ✨ ویژگی‌ها

### 1. قالب‌بندی حرفه‌ای
- **جدول با استایل استاندارد**: استفاده از استایل `Light Grid Accent 1`
- **رنگ‌بندی هدر**: هدر جدول با رنگ آبی و متن سفید
- **ردیف‌های متناوب**: رنگ‌بندی خاکستری روشن برای خوانایی بهتر
- **فونت فارسی**: استفاده از فونت `B Nazanin` برای متن‌های فارسی

### 2. پشتیبانی کامل از فارسی
- راست‌چین کردن خودکار متن
- تنظیم حاشیه‌ها برای راست به چپ
- پشتیبانی از کاراکترهای ویژه فارسی

### 3. حذف هوشمند تکراری‌ها
```python
# نرمال‌سازی نام محصول
normalized_name = product_name.strip().lower()

# شناسایی تکراری (بدون حساسیت به حروف بزرگ/کوچک و فاصله)
if normalized_name in seen_products:
    duplicates_found += 1
    continue
```

### 4. اطلاعات متا
هر سند Word شامل:
- تاریخ و ساعت ایجاد
- نام فایل منبع CSV
- آمار کامل تبدیل

### 5. گزارش‌دهی دقیق
```
[آمار تبدیل]
  • تعداد کل ردیف‌های پردازش شده: 9
  • تعداد محصولات یکتا نوشته شده: 6
  • تعداد محصولات تکراری حذف شده: 2
```

---

## 📝 مثال‌های کاربردی

### مثال 1: تبدیل ساده

**فایل ورودی:** `products.csv`
```csv
نام محصول,قیمت
آیفون 14,50000000
سامسونگ گلکسی,30000000
```

**دستور:**
```shell
python csv-to-word.py
> H:\Repo\WordpressDevelopment\Products-Price-Exporter\products.csv
```

**خروجی:** `products.docx` در همان پوشه

---

### مثال 2: فایل با مسیر دارای فاصله

**مسیر فایل:** `C:\My Documents\Products List\items.csv`

```shell
python csv-to-word.py
> "C:\My Documents\Products List\items.csv"
```

یا بدون گیومه:
```shell
python csv-to-word.py
> C:\My Documents\Products List\items.csv
```

**توجه:** برنامه خودکار گیومه‌ها را حذف می‌کند.

---

### مثال 3: فایل CSV با تکراری

**فایل ورودی:** `test_sample.csv`
```csv
نام محصول,قیمت,دسته‌بندی
آیفون 14,50000000,موبایل
سامسونگ گلکسی,30000000,موبایل
  آیفون 14  ,51000000,موبایل
IPHONE 14,52000000,موبایل
```

**نتیجه:**
- ردیف اول: `آیفون 14` → ✅ نوشته می‌شود
- ردیف سوم: `  آیفون 14  ` → ❌ حذف می‌شود (تکراری)
- ردیف چهارم: `IPHONE 14` → ❌ حذف می‌شود (تکراری)

**آمار:**
```
تعداد محصولات یکتا: 2
تعداد تکراری‌های حذف شده: 2
```

---

## 📊 ساختار فایل CSV ورودی

### فرمت استاندارد

```csv
ستون1,ستون2,ستون3,...
مقدار1,مقدار2,مقدار3,...
مقدار1,مقدار2,مقدار3,...
```

### مثال واقعی

```csv
نام محصول,قیمت,دسته‌بندی
آیفون 14 پرو,55000000,موبایل
لپتاپ HP,25000000,کامپیوتر
ماوس بی‌سیم,500000,لوازم جانبی
```

### نکات مهم
1. ✅ **ردیف اول**: هدر جدول (عنوان ستون‌ها)
2. ✅ **جداکننده**: کاما (`,`)
3. ✅ **کدگذاری**: UTF-8 (با یا بدون BOM)
4. ✅ **ردیف‌های خالی**: خودکار نادیده گرفته می‌شوند
5. ✅ **فاصله‌های اضافی**: در نام محصولات trim می‌شوند

---

## 📄 ساختار فایل Word خروجی

### بخش‌های سند

#### 1. عنوان اصلی
```
لیست محصولات و قیمت‌ها
```
- فونت: B Nazanin، اندازه 18، Bold
- رنگ: آبی تیره (#003366)
- تراز: راست‌چین

#### 2. اطلاعات متا
```
تاریخ ایجاد: 2026-02-23 19:33:00
فایل منبع: test_sample.csv
```
- فونت: B Nazanin، اندازه 10
- رنگ: خاکستری (#646464)

#### 3. جدول داده‌ها
| نام محصول | قیمت | دسته‌بندی |
|-----------|------|-----------|
| آیفون 14 | 50000000 | موبایل |
| سامسونگ گلکسی | 30000000 | موبایل |

**ویژگی‌های جدول:**
- هدر: پس‌زمینه آبی (#0070C0)، متن سفید
- ردیف‌های زوج: پس‌زمینه خاکستری روشن (#E7E6E6)
- ردیف‌های فرد: پس‌زمینه سفید
- تمام سلول‌ها: فونت B Nazanin، اندازه 11

#### 4. پاورقی آماری
```
══════════════════════════════════════
آمار تبدیل:
• تعداد کل ردیف‌های پردازش شده: 9
• تعداد محصولات یکتا: 6
• تعداد محصولات تکراری حذف شده: 2
══════════════════════════════════════
```

---

## 🔧 عیب‌یابی

### مشکل 1: خطای "ModuleNotFoundError: No module named 'docx'"

**علت:** کتابخانه `python-docx` نصب نشده است.

**راه‌حل:**
```shell
pip install python-docx
```

---

### مشکل 2: خطای "FileNotFoundError"

**علت:** مسیر فایل CSV اشتباه است.

**راه‌حل:**
1. مطمئن شوید مسیر کامل (Absolute Path) را وارد کرده‌اید
2. از وجود فایل اطمینان حاصل کنید:
   ```shell
   dir "H:\Repo\...\your_file.csv"  # Windows
   ls -la /path/to/your_file.csv     # Linux/macOS
   ```

---

### مشکل 3: خطای "UnicodeDecodeError"

**علت:** فایل CSV با کدگذاری غیر UTF-8 ذخیره شده است.

**راه‌حل:**
1. فایل CSV را در Excel یا Notepad++ باز کنید
2. با کدگذاری UTF-8 ذخیره کنید:
   - **Excel**: File → Save As → CSV UTF-8
   - **Notepad++**: Encoding → Convert to UTF-8

---

### مشکل 4: فونت‌های فارسی نمایش داده نمی‌شوند

**علت:** فونت `B Nazanin` در سیستم نصب نیست.

**راه‌حل:**
1. فونت B Nazanin را دانلود و نصب کنید
2. یا در کد، فونت را به فونت دیگری تغییر دهید:
   ```python
   run.font.name = 'Arial'  # یا 'Tahoma'
   ```

---

### مشکل 5: فایل Word باز نمی‌شود

**علت:** ممکن است Word نصب نباشد یا فایل خراب باشد.

**راه‌حل:**
1. از Microsoft Word یا LibreOffice استفاده کنید
2. آنتی‌ویروس را موقتاً غیرفعال کنید
3. فایل را دوباره ایجاد کنید

---

## ❓ سوالات متداول

### 1. آیا می‌توانم چندین فایل را همزمان تبدیل کنم؟

خیر، نسخه فعلی فقط یک فایل را در هر اجرا پردازش می‌کند. برای تبدیل چندین فایل، می‌توانید از یک حلقه در خط فرمان استفاده کنید:

**Windows PowerShell:**
```powershell
Get-ChildItem *.csv | ForEach-Object {
    echo $_.FullName | python csv-to-word.py
}
```

**Linux/macOS Bash:**
```bash
for file in *.csv; do
    echo "$file" | python csv-to-word.py
done
```

---

### 2. چگونه می‌توانم استایل جدول را تغییر دهم؟

در خط 147 کد، استایل جدول را تغییر دهید:

```python
table.style = 'Light Grid Accent 1'  # استایل فعلی
```

استایل‌های دیگر:
- `'Medium Grid 1 Accent 1'`
- `'Colorful Grid Accent 1'`
- `'Light Shading Accent 1'`

لیست کامل: [Microsoft Word Table Styles](https://python-docx.readthedocs.io/en/latest/user/styles-understanding.html)

---

### 3. آیا می‌توانم فایل خروجی را در پوشه دیگری ذخیره کنم؟

در حال حاضر، فایل خروجی در همان پوشه فایل CSV ذخیره می‌شود. برای تغییر، خط 82 را ویرایش کنید:

```python
# قبل:
output_docx = input_csv.with_suffix(".docx")

# بعد:
output_docx = Path("C:/Output/Folder") / input_csv.with_suffix(".docx").name
```

---

### 4. چرا برخی رکوردها حذف می‌شوند؟

رکوردها در موارد زیر حذف می‌شوند:
1. **تکراری بودن**: نام محصول تکراری است (بدون حساسیت به حروف بزرگ/کوچک)
2. **خالی بودن**: تمام فیلدهای ردیف خالی هستند
3. **بدون نام محصول**: ستون اول (نام محصول) خالی است

---

### 5. آیا می‌توانم فرمت تاریخ را تغییر دهم؟

بله، در خط 115:

```python
# قبل:
info_run = info_para.add_run(f"تاریخ ایجاد: {datetime.now().strftime('%Y-%m-%d %H:%M:%S')}\n")

# بعد (شمسی):
from persiantools.jdatetime import JalaliDate
jalali_date = JalaliDate.today().strftime('%Y/%m/%d')
info_run = info_para.add_run(f"تاریخ ایجاد: {jalali_date}\n")
```

**نیاز به نصب:**
```shell
pip install persiantools
```

---

### 6. حجم فایل Word خیلی زیاد است، چه کار کنم؟

حجم فایل به تعداد رکوردها بستگی دارد. برای کاهش حجم:

1. **حذف استایل پیچیده**: خطوط 173-176 را کامنت کنید
2. **استفاده از فونت سبک‌تر**: فونت Arial به جای B Nazanin
3. **حذف رنگ‌بندی**: خطوط 199-202 را حذف کنید

---

## 📚 منابع بیشتر

- [مستندات python-docx](https://python-docx.readthedocs.io/)
- [راهنمای CSV در Python](https://docs.python.org/3/library/csv.html)
- [آموزش pathlib](https://docs.python.org/3/library/pathlib.html)

---

## 🤝 مشارکت و پشتیبانی

اگر مشکلی پیدا کردید یا پیشنهادی دارید:
1. Issue باز کنید
2. Pull Request ارسال کنید
3. با ما در ارتباط باشید

---

## 📜 لایسنس

این پروژه تحت لایسنس MIT منتشر شده است.

---

**نسخه:** 1.0.0  
**تاریخ به‌روزرسانی:** 2026-02-23  
**نویسنده:** Products Price Exporter Team

---

**موفق باشید! 🚀**
